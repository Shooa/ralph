# Ralph — Autonomous Agent Loop

Implement → Review loop. Agent stages, reviewer commits.
Key files: `ralph.sh`, `CLAUDE.md` (implement prompt), `review-prompt.md` (review prompt), `prd.json` (stories).
